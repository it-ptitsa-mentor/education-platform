---
title: "Virtual Dom"
module: "Модуль 5"
topic: "JS: React"
buildin_id: 48b423b2-4425-40c9-96a1-3fa31b9643d2
source: platform
rewritten_at: 2026-06-24
reviewed_by:
---

# Virtual Dom

В одном из прошлых модулей вы уже видели, как DOM меняется при работе со страницей. Подход React сильно отличается от прямого DOM API: главное — как обновляется уже отрисованный интерфейс. При ручных манипуляциях обычно делают так:

1. Удалить из DOM то, что стало неактуально для следующего состояния.

1. Изменить, если надо, те элементы, которые присутствуют на экране и должны остаться.

1. Добавить новые элементы на страницу (точечно).

Иными словами, чтобы перейти к новому состоянию, нужно знать предыдущее и править его точечно.

В React картина другая. После любого изменения и вызова `setState` формируется новое представление, и компоненты отрисовываются так, будто с нуля. Фактически дерево пересчитывается заново: не важно, что было на экране до этого.

Авторы React называют это *one-way data flow*:

1. Действия пользователя приводят к изменению состояния приложения (через `setState`).

1. React запускает цикл отрисовки. Начиная от того компонента, в котором было изменено состояние (как правило, корневой компонент), через пропсы данные постепенно распространяются от компонентов более высокого уровня до самых глубинных компонентов.

1. Получившийся `html` интегрируется в страницу.

<!-- IMG (из Buildin, перезалить отдельно) -->
Знакомым с функциональным стилем это напоминает иммутабельную модель: React делает «снимок» UI и строит следующий независимо. Наивная реализация — `mountElement.innerHTML` после каждого `setState`, заменяющая разметку целиком (см. доп. материалы); в сотнях строк можно набросать мини-библиотеку с похожей идеей.

Проблема `innerHTML` — производительность: операция очень дорогая. Поэтому в React выбрали другой путь.

## Дерево виртуального DOM

Ранее в курсе говорилось, что компоненты отрисовываются, и это немного не так. В реальности после того, как отработает их рендеринг (вызов функции `render` для всего дерева компонентов), создаётся так называемый *виртуальный DOM* (virtual DOM). Это просто JS-объект определённой структуры, который отражает состояние экрана. Далее React сравнивает новое дерево виртуального DOM со старым и строит разницу между ними («дифф», или объект, описывающий разницу между старым и новым состоянием). И только в этот момент начинается отрисовка нового состояния в реальный DOM. Здесь уже должно быть понятно, что React умнее, чем кажется на первый взгляд, и вносит изменения в реальный DOM настолько эффективно, насколько это возможно, ведь он знает КАК его надо изменить.

<!-- IMG (из Buildin, перезалить отдельно) -->
Отсюда следствие: реальный DOM под управлением React (потомки узла, куда монтируется корень) нельзя менять снаружи. Иначе React теряет актуальную картину DOM для diff и предупреждает о конфликте.

Важно: виртуальный DOM — не самоцель React, как многие думают. Это просто эффективный способ реализовать идею *one-way data flow*. Если бы работал вариант с `innerHTML`, то никто бы не делал виртуальный DOM.

Построение JS-объекта дешевле работы с настоящим DOM, но при большом дереве diff всё же может тормозить интерфейс — об этом в отдельном уроке.

## Дополнительные материалы

- [Почему сложно программировать UI (Кирилл Мокевнин)](https://youtu.be/DCeNCr2tKOI)

## **Далее → **
