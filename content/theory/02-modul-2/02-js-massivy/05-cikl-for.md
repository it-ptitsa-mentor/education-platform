---
title: "Цикл for"
module: "Модуль 2"
topic: "JS: Массивы"
buildin_id: f9dd0a67-3cbd-4469-aa6b-c389c0eec9b6
source: platform
rewritten_at: 2026-06-24
reviewed_by:
---

# Цикл for

С массивами почти всегда приходится обрабатывать сразу все элементы: вывести список, посчитать что-то, проверить данные. Нужен механизм перебора — самый прямой путь к нему цикл `for`.

## Обход

Цикл сам по себе не «про массивы», но у него есть счётчик, который удобно использовать как индекс. Связать их несложно:

```
// Создаем массив
const userNames = ['petya', 'vasya', 'evgeny']

// Определяем цикл
// Начальное значение счетчика i = 0 – вычисляется один раз перед началом выполнения
// Условие остановки i < userNames.length – выполняется перед каждой итерацией
// Изменение счетчика i += 1 – выполняется после каждой итерации
for (let i = 0; i < userNames.length; i += 1) {
  // Этот код выполняется для каждого элемента
  console.log(userNames[i])
}

// => 'petya'
// => 'vasya'
// => 'evgeny'
```

Здесь массив из трёх имён; в цикле каждое имя выводится на отдельной строке (`console.log` сам добавляет перевод строки).

При обходе `for` счётчик обычно и есть индекс: стартует с нуля и растёт до `userNames.length - 1` — индекса последнего элемента. Поэтому в условии `i < userNames.length` строгое сравнение `<`, а не `<=`.

А если нужен обратный порядок? Есть два приёма. Первый — идти вперёд по счётчику, а индекс брать по формуле `размер массива - 1 - текущее значение счетчика`:

```
const userNames = ['petya', 'vasya', 'evgeny']

for (let i = 0; i < userNames.length; i += 1) {
  const index = (userNames.length - 1) - i
  console.log(userNames[index])
}
```

Второй — сразу идти от последнего индекса к нулю:

```
const userNames = ['petya', 'vasya', 'evgeny']

// Начальное значение i соответствует последнему индексу в массиве
for (let i = userNames.length - 1; i >= 0; i -= 1) {
  console.log(userNames[i])
}
```

При таком обходе условие остановки — `>=`, иначе элемент с индексом 0 не попадёт в цикл.

## Изменение

Во время обхода массив можно не только читать, но и менять. Допустим, нужно привести email-адреса к нижнему регистру:

```
const emails = ['VASYA@gmAil.com', 'iGoR@yandex.RU', 'netiD@hot.CoM']

console.log(emails)
// => [ 'VASYA@gmAil.com', 'iGoR@yandex.RU', 'netiD@hot.CoM' ]

for (let i = 0; i < emails.length; i += 1) {
  const email = emails[i]

  // toLowerCase() — стандартный метод js,
  // преобразующий строку в нижний регистр
  const normalizedEmail = email.toLowerCase()
  // Заменяем значение
  emails[i] = normalizedEmail
}

console.log(emails)
// => [ 'vasya@gmail.com', 'igor@yandex.ru', 'netid@hot.com' ]
```

Ключевая строка: `emails[i] = normalizedEmail;` — перезапись элемента под индексом `i`.

## Резюме

`for` с массивами сочетается гибко: не обязательно идти от начала до конца целиком — можно брать каждый второй элемент, останавливаться на половине и т.д., по задаче.

То же относится и к `while`: массивам нужен лишь индекс.

---

### Дополнительные материалы

1. [Официальная документация](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Statements/for)

## Далее →
