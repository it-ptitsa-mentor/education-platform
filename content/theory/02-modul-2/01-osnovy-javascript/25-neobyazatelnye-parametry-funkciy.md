---
title: "Необязательные параметры функций"
module: "Модуль 2"
topic: "Основы JavaScript"
buildin_id: ea76bb96-76d8-4b1f-b7b2-04c80cc2e81e
source: platform
rewritten_at: 2026-06-24
reviewed_by:
---

# Необязательные параметры функций

Часто параметр почти всегда один и тот же — тогда задают **значение по умолчанию** и при необходимости переопределяют его при вызове:

```
// Функция возведения в степень
// Второй параметр имеет значение по умолчанию 2
const pow = (x, exponent = 2) => {
  return x ** exponent
}
// 3 во второй степени (двойка задана по умолчанию)
pow(3) // 9
// три в третьей степени
pow(3, 3) // 27
```

В определении это выглядит как `param = значение`: подставляется только если аргумент не передали.

<!-- IMG (из Buildin, перезалить отдельно) -->
Дефолт возможен и у единственного параметра:

```
const print = (text = 'nothing') => console.log(text)

print() // nothing
print('World') // World
```

Дефолтов может быть несколько:

```
const f = (a = 5, b = 10, c = 100) => { ... }
```

Параметры с дефолтом ставят **в конец** списка: аргументы при вызове заполняют параметры слева направо. Иначе дефолты перезапишутся или обязательный параметр останется без значения. Примеры:

```
// Вызовем эту функцию со следующими аргументами: f(1, 2, 3)
const f = (a = 5, b = 10, c = 100, x) => { ... }
// параметры получат следующие значения: а = 1, b = 2, c = 3, x = undefined
// в x мы ничего не передали, и JS присвоил ему значение undefined

// Именно поэтому параметры со значением по умолчанию должны идти справа от обычных,
// иначе они либо будут перезаписаны, либо обычный параметр может остаться без значения

// Вызываем функцию, f(1, 2)
const f = (a = 5, x, b = 10, c = 100) => { ... }
// a = 1, x = 2, остальные параметры получают значения по умолчанию

// Здесь все хорошо, никаких неожиданностей
const f = (x, a = 5, b = 10, c = 100) => { ... }

// И здесь
const f = (x, y, a = 5, b = 10, c = 100) => { ... }
```

## Далее →
