---
title: "Агрегация"
module: "Модуль 3"
topic: "JS: Деревья"
buildin_id: 7a835427-f6be-487e-995a-6ac6868e217c
source: platform
rewritten_at: 2026-06-26
reviewed_by:
---

# Агрегация

**Агрегация** — главная операция при работе с деревьями: общее число файлов в каталоге, суммарный размер, список всех файлов, поиск по шаблону — всё это агрегирование.

Суть — накапливать результат по ходу обхода. Хорошо подходит DFS с рекурсией из прошлого урока: обходим узлы и собираем ответ снизу вверх.

Пример — подсчёт всех узлов (файлов и директорий) в дереве:

```javascript
const tree = fsTrees.mkdir('/', [
  fsTrees.mkdir('etc', [
    fsTrees.mkfile('bashrc'),
    fsTrees.mkfile('consul.cfg'),
  ]),
  fsTrees.mkfile('hexletrc'),
  fsTrees.mkdir('bin', [
    fsTrees.mkfile('ls'),
    fsTrees.mkfile('cat'),
  ]),
])

// В реализации используем рекурсивный процесс,
// чтобы добраться до самого дна дерева
const getNodesCount = (tree) => {
  if (fsTrees.isFile(tree)) {
    // Возвращаем `1` для учёта текущего файла
    return 1
  }

  // Если узел — директория, получаем его потомков
  const children = fsTrees.getChildren(tree)
  // Здесь начинается самая сложная часть
  // Считаем количество потомков для каждого из потомков,
  // рекурсивно вызывая нашу функцию `getNodesCount`
  const descendantCounts = children.map(getNodesCount)
  // Возвращаем `1` (текущая директория) + общее количество потомков
  return 1 + _.sum(descendantCounts)
}

getNodesCount(tree) // 8
```

Кода мало, но логика плотная. Ключевые шаги:

1. Если узел — файл, возвращаем `1`.

1. Если директория — берём детей и для каждого снова вызываем ту же функцию.

1. Каждый рекурсивный вызов даёт число; из них складывается массив, который нужно суммировать.

1. Итог: сумма по детям плюс `1` за текущий узел.

Перед следующим уроком стоит поиграть с этим кодом в консоли — так проще «прочувствовать» рекурсию.

## Далее →
