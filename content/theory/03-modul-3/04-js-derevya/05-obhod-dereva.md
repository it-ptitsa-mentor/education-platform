---
title: "Обход дерева"
module: "Модуль 3"
topic: "JS: Деревья"
buildin_id: f277f318-76e3-414d-ade3-cc69c615ab29
source: platform
rewritten_at: 2026-06-26
reviewed_by:
---

# Обход дерева

**Обход дерева** — пошаговый перебор узлов по связям «родитель — потомок». Обычно каждый узел посещается ровно один раз. Как и у любой коллекции, можно использовать цикл или рекурсию; у деревьев вариантов порядка обхода больше, чем «слева направо» у списка.

В курсе используется **обход в глубину** (DFS): он естественно получается при рекурсивном обходе. Другие стратегии описаны, например, в [статье Википедии об обходе дерева](https://ru.wikipedia.org/wiki/%D0%9E%D0%B1%D1%85%D0%BE%D0%B4_%D0%B4%D0%B5%D1%80%D0%B5%D0%B2%D0%B0).

## Обход в глубину (Depth-first search)

Один из способов обойти дерево (в общем случае — граф). Идея: углубляться в одно поддерево как можно дальше, прежде чем переходить к соседним веткам. Такой порядок хорошо ложится на рекурсию.

<!-- IMG (из Buildin, перезалить отдельно) -->
Пример на таком дереве:

```javascript
//     * A
//   / | \
// B * C * D
//  /|   |\
// E F   G J
```

Нелистовые вершины отмечены `*`. Старт — с корня.

1. Проверяем, есть ли у A дети. Если да — для каждого ребёнка рекурсивно запускаем тот же обход;

1. В первом рекурсивном вызове оказываемся в поддереве:

```javascript
// B *
//  /|
// E F
```

Повторяем шаг 1 и уходим на уровень ниже.

1. Доходим до листа `E`: детей нет, выполняем нужную работу и возвращаемся вверх.

1. Снова узел B — первый ребёнок обработан, рекурсия заходит в `F`, затем подъём к корню и обход остальных веток.

```javascript
const tree = fsTrees.mkdir('/', [
  fsTrees.mkdir('etc', [
    fsTrees.mkfile('bashrc'),
    fsTrees.mkfile('consul.cfg'),
  ]),
  fsTrees.mkfile('hexletrc'),
  fsTrees.mkdir('bin', [
    fsTrees.mkfile('ls'),
    fsTrees.mkfile('cat'),
  ]),
])

const dfs = (tree) => {
  // Распечатываем содержимое узла
  console.log(fsTrees.getName(tree))
  // Если это файл, то возвращаем управление
  if (fsTrees.isFile(tree)) {
    return
  }

  // Получаем детей
  const children = fsTrees.getChildren(tree)

  // Применяем функцию dfs ко всем дочерним элементам
  // Множество рекурсивных вызовов в рамках одного вызова функции
  // называется древовидной рекурсией
  children.forEach(dfs)
}

dfs(tree)
// => /
// => etc
// => bashrc
// => consul.cfg
// => hexletrc
// => bin
// => ls
// => cat
```

`console.log` здесь только для наглядности. На практике важнее изменение дерева или **агрегация** данных по нему — агрегацию разберём позже, сейчас — изменение.

Допустим, нужно сменить владельца у всего дерева — всех файлов и каталогов. Соединяем рекурсию выше и обновление узлов из прошлого урока:

```javascript
const changeOwner = (tree, owner) => {
  const name = fsTrees.getName(tree)
  const newMeta = _.cloneDeep(fsTrees.getMeta(tree))
  newMeta.owner = owner

  if (fsTrees.isFile(tree)) {
    // Возвращаем обновлённый файл
    return fsTrees.mkfile(name, newMeta)
  }
  // Дальше идет работа, если директория

  const children = fsTrees.getChildren(tree)
  // Ключевая строчка
  // Вызываем рекурсивное обновление каждого ребёнка
  const newChildren = children.map(child => changeOwner(child, owner))
  const newTree = fsTrees.mkdir(name, newChildren, newMeta)

  // Возвращаем обновлённую директорию
  return newTree
}

// Эту функцию можно обобщить до map (отображения), работающего с деревьями
```

Отличие от первого примера: вместо печати собираются новые узлы и из них — новое дерево.

Дальше по курсу почти всё **опирается на этот алгоритм**. Попробуйте в редакторе написать `changeOwner` без подглядывания — так проще убедиться, что схема понятна.

## Далее →
