import { describe, it, expect } from "vitest";

describe("regexp-char-classes", () => {
  it("placeholder — оригинальные тесты Hexlet пока не импортированы", () => {
    expect(true).toBe(true);
  });
});
