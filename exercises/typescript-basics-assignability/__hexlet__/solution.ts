// @ts-check
type Form = {
  age: {
    value: number
    validator: (val: number) => boolean
  }
  name: {
    value: string
    validator: (val: string) => boolean
  }
}

const form: Form = {
  // BEGIN (write your solution here)
  
  // END
}

export default form
