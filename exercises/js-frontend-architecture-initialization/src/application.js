// Основная логика счётчика уже написана ниже.
// Добавьте инициализацию состояния (с учётом initialState)
// и экземпляра i18next, затем раскомментируйте код

// import i18next from 'i18next';
// import en from './locales/en.js';
// import ru from './locales/ru.js';

export default async (container, initialState = {}) => {
  // Значения по умолчанию:
  // {
  //   language: { lng: 'en' },
  //   data: { clicksCount: 0 },
  // }
};
